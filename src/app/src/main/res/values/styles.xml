<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Minimal" parent="AppTheme">
        <item name="android:buttonStyle">@style/MinButton</item>
        <!--<item name="android:textColor">@android:color/white</item>-->
    </style>

    <style name="AllMinButtons" parent="AppTheme">
        <item name="android:buttonStyle">@style/MinButton</item>
    </style>

    <style name="MinButton" parent="Widget.AppCompat.Button.Borderless">
        <!--<item name="android:background">@color/colorMinButtonBackground</item>-->
        <item name="android:textColor">@android:color/white</item>
        <item name="colorButtonNormal">@color/colorMinButtonBackground</item>
        <item name="android:gravity">center</item>
    </style>
</resources>